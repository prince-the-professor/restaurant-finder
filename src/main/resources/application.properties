spring.application.name=restaurant-finder
# === Spring Boot App Settings ===
server.port=8080
# === Elasticsearch Configuration ===
spring.elasticsearch.uris=http://localhost:9200
#spring.elasticsearch.username=elastic
#spring.elasticsearch.password=changeme  # Replace if you changed it during setup
# === Optional Logging (for debugging ES) ===
#logging.level.org.springframework.data.elasticsearch.client=DEBUG
logging.level.org.springframework.data.elasticsearch.client=OFF
